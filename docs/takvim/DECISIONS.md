# Narbulut Takvim — Karar Defteri

Bu dosya, proje boyunca alınan **bağlayıcı** kararları tutar.
Kural: bir karar buraya yazıldıysa, sonraki fazlar onu yeniden tartışmaz — ancak açıkça revize edilebilir.

| ID | Konu | Karar | Neden | Tarih | Kaynak |
|---|---|---|---|---|---|
| **D-001** | Kanıt seviyesi | Tüm dokümanlarda üç seviyeli kanıt etiketi kullanılacak: **OBSERVED** (ekran/not ile doğrudan doğrulanmış) · **INFERRED** (kuvvetli çıkarım, doğrulanmamış) · **UNKNOWN** (yeterli kanıt yok). `[I]` ve `[U]` maddeleri doğrulanmadan requirement'a dönüştürülmez. | rev.1 audit'te gözlem ile ürün varsayımı iç içe geçmişti; karar kalitesi kanıt kalitesine bağlı. | 2026-08-27 | Kullanıcı |
| **D-002** | Ürün scope'u | Bu çalışma **genel Narbulut Takvim ürününün** Product / UX / Frontend redesign'ıdır. **AKM bir reference customer'dır, requirement kaynağı değildir.** AKM'nin kamu kurumu yapısı, Personel/Birim Amiri/Yönetici rolleri, onay akışı ve denetim kaydı ihtiyaçları otomatik requirement sayılmaz; FAZ 2 ve FAZ 3'te ayrıca değerlendirilir. | Elimizdeki materyalin tamamı AKM kaynaklı; tek müşterinin ihtiyacının genel ürüne requirement olarak sızma riski vardı. | 2026-08-27 | Kullanıcı |
| **D-003** | Panel/rol kapsamı | Ekran görüntülerinin **Bayi** hesabından alınmış olması, redesign'ın Bayi paneline yapılacağı anlamına gelmez. Panel ve rol kapsamı **FAZ 3'te** netleştirilecek. O zamana kadar hiçbir doküman panel hedefi hakkında varsayım yapmaz. | rev.1 audit'te "Bayi hesabı" gözlemi kapsam çıkarımına dönüşme riski taşıyordu. | 2026-08-27 | Kullanıcı |
| **D-004** | Faz yapısı | Faz yapısına **FAZ 1.5 — Evidence Completion + Audit Cleanup** eklendi. FAZ 1 kapatılmadan FAZ 2'ye geçilmeyecek. | Mevcut etkinlik düzenleme deneyimi hiç görülmeden benchmark yapılırsa, zaten var olan bir davranış "eksik" olarak rakip listesine girebilirdi. | 2026-08-27 | Kullanıcı (Claude önerisi üzerine) |
| **D-005** | FAZ 1 çıktısının niteliği | FAZ 1 audit'i **yalnızca tespit** içerir. Çözüm, benchmark, rakip referansı, yeni özellik önerisi ve öncelik sıralaması (P0/P1/P2) FAZ 1 çıktısına dahil edilmez. | Tespit ile çözümün karışması, seçim yapılmadan kapsamın büyümesine yol açar. | 2026-08-27 | Kullanıcı |
| **D-006** | Redesign prensibi | Mevcut sistem **referans + problem kaynağı + ürün bağlamıdır**; yeni tasarımın sınırı değildir. Mevcut sistemden yalnızca gerekli iş mantığı, kullanıcı kavramları, platformla ilişkili yapı taşları ve korunması gereken faydalı fikirler devralınır. **Kötü UX kalıpları legacy davranış diye korunmaz.** Her ana alan için sorulacak soru "mevcut sistem bunu nasıl yapıyordu?" değil, **"Narbulut kullanıcısı bu işi en iyi nasıl yapar?"** olacaktır. | Mevcut frontend zaten görsel ve deneyim olarak yetersiz bulunduğu için yeniden ele alınıyor; birebir reverse-engineering hem gereksiz hem de kötü kalıpları taşır. | 2026-08-27 | Kullanıcı |
| **D-007** | UNKNOWN yönetimi | UNKNOWN maddeleri iki gruba ayrılır. **Grup A (redesign-kritik):** bilinmemesi yeni ürün kararını ciddi etkiler → FAZ 3 Requirement Discovery'de sorulur. **Grup B (legacy detay):** yalnızca eski frontend'in davranışıyla ilgili → **çözmek için zaman harcanmaz.** Hiçbir UNKNOWN FAZ 2'yi bloke etmez. | Her UNKNOWN'ı kapatmaya çalışmak, mevcut sistemi birebir belgelemeye dönüşür ve D-006 ile çelişir. Audit'in amacı yeterli ürün bağlamı sağlamaktır, kusursuz dokümantasyon değil. | 2026-08-27 | Kullanıcı |
| **D-008** | Ek test verisi | Kanıt toplamak için mevcut üründe gereksiz test verisi üretilmeyecek, sistem zorlanmayacak. Erişilemeyen ekran UNKNOWN kalır. | Canlı/paylaşımlı bir ortamda kirli veri bırakmamak. | 2026-08-27 | Kullanıcı |
| **D-009** | FAZ 1 kapanışı | Audit **rev.3 ile FINAL** olarak kapatıldı. Kalan UNKNOWN'lar (Grup A: 13, Grup B: 19) audit'i bloke etmez. | Yeterli ürün bağlamı sağlandı; G-4 ve G-5 ile en kritik iki boşluk kapandı. | 2026-08-27 | Claude, kullanıcı onayına sunuldu |
| **D-010** | FAZ 1 kapanışı onayı | Kullanıcı FAZ 1 ve FAZ 1.5'i onayladı. **`00-current-state-audit.md` rev.3 FINAL, Current State için tek ana referanstır.** FAZ 1 kapanmıştır; yeniden açılmayacak. | Sonraki fazların sabit bir zemine dayanması için. | 2026-08-27 | Kullanıcı |
| **D-011** | Benchmark dili ve kanıt kuralları | Benchmark bulguları **ürün/sektör standardı gibi sunulamaz**. "Evrensel", "istisnasız", "her üründe", "sektör standardı" gibi ifadeler, tüm araştırılan ürünlerde birebir doğrulanmadıkça kullanılmaz; yerine "bu benchmark setinde ortak", "araştırılan örneklerin çoğunda", "güçlü ortak yaklaşım" yazılır. Ayrıca: **(a)** ürün-spesifik implementation, genel UX pattern'inden ayrılır ve Narbulut'un zorunlu mimarisi gibi sunulmaz; **(b)** plana/admin ayarına/tenant'a bağlı davranışlar açıkça koşuluyla işaretlenir; **(c)** Robin ve Envoy bulguları `[O-zayıf]`'tır ve yalnızca bunlara dayanan bir pattern "güçlü benchmark sonucu" sayılmaz. | rev.1'deki gözlem/varsayım karışması riskinin benchmark tarafındaki karşılığı. Zayıf kanıtlı bir bulgunun ürün kararına dönüşmesi en pahalı hata türü. | 2026-08-27 | Kullanıcı |
| **D-012** | Pattern ≠ Feature | Küçük UX pattern'i ile büyük subsystem geliştirmesi **aynı seviyede değerlendirilmez**. Örnek: *"müsaitlik seçim anında görünür olmalı"* bir pattern; *Scheduling Assistant* bir subsystem. Ayrım `01-competitor-capability-map.md` Bölüm 13'te tablolandı. | Kapsam kararlarının, gerçek büyüklüğü gizleyen bir listede alınmasını önlemek. | 2026-08-27 | Kullanıcı |
| **D-013** | Candidate oluşturma eşiği | **"Rakipte var" tek başına candidate oluşturmak için yeterli değildir.** Her candidate mümkün olduğunca `00-current-state-audit.md` içindeki bir problemle bağlanır; audit karşılığı olmayan adaylar açıkça işaretlenir. | Benchmark'ın kapsam şişirmesini önlemek. | 2026-08-27 | Kullanıcı |
| **D-014** | Problem cluster yaklaşımı | Audit problemleri tek tek candidate'a çevrilmez; önce **problem cluster'larına** ayrılır ve zincir ilişkileri (ör. Oda Veri Modeli → Oda Keşfi; Free/busy → Scheduling Assistant) korunur. Çözüm alternatifleri, bağımsız özellikler olarak değil, **aynı problemin 2–3 kapsam seviyesi** olarak sunulur. | Birbirine bağlı halkaların bağımsız özellik gibi oylanması yanlış kapsam kararı üretir. | 2026-08-27 | Kullanıcı |
| **D-015** | FAZ 2A kapanışı | `01-competitor-capability-map.md` **v2 FINAL**. Kanıt/dil temizliği tamamlandı. | — | 2026-08-27 | Kullanıcı onayı + cleanup |
| **D-016** | Cluster kategorileri | Cluster'lar dört kategoriye ayrılır: **Direct UX / Product Problems** · **Foundation / Enablers** · **Cross-cutting Capabilities** · **Design Brief Inputs**. **Bir foundation son kullanıcı feature'ı gibi değerlendirilmez**; değeri, mümkün kıldığı üst seviye deneyimler üzerinden ölçülür. Foundation olanlar: **PC-06** (Çalışma Zamanı Kuralları), **PC-08** (Oda Veri Modeli), **PC-11** (İzin Modeli). Bağımlılık zincirleri korunur. | Foundation'ı feature gibi oylamak, düşük "kullanıcı değeri" puanı alıp elenmesine ve üstündeki her şeyin imkânsızlaşmasına yol açar. | 2026-08-27 | Kullanıcı |
| **D-017** | PC-14 Arama | Ayrı cluster olarak kalıyor (🟨 Cross-cutting). PC-01/PC-08 altına dağıtılmayacak. | Arama bir yüzey değil, modül geneline yayılan bir yetenek; dağıtılırsa iki yerde yarım çözülür. | 2026-08-27 | Kullanıcı (S1) |
| **D-018** | PC-15 Tasarım Dili | FAZ 2B'de **candidate ve kapsam seviyesi üretilmeyecek**. 🟪 **FAZ 6 — Design Brief Input** olarak işaretlendi. | İçeriğinin tamamı tasarım yönü kararı; A/B/C kapsamı bu aşamada anlamsız. | 2026-08-27 | Kullanıcı (S2) |
| **D-019** | PC-10 Rezervasyon Onayı | FAZ 2B'de kalıyor ancak **⚠️ CONDITIONAL — FAZ 3 BUSINESS VALIDATION REQUIRED** statüsünde. **FAZ 3'te iş gereksinimi doğrulanmadan seçilmiş özellik kabul edilmeyecek.** | Cluster'ın tamamı AKM'nin kamu kurumu ihtiyacından doğuyor (D-002). Yine de kapsamı görülüyor, çünkü PC-08'in kapsam kararı PC-10'un gerekip gerekmediğine bağlı. | 2026-08-27 | Kullanıcı (S3) |
| **D-020** | UX-16 ataması | UX-16 (boş "Takvimlerim" durumu açıklamasız) **PC-12 System States** altına bağlandı. | Doğası bir boş-durum/geri bildirim problemi. | 2026-08-27 | Kullanıcı |
| **D-021** | ⭐ işaretlerinin statüsü | `03-solution-options.md` içindeki **⭐ işaretleri Claude'un önerisidir, seçilmiş kapsam değildir.** Kullanıcı açıkça seçim yapmadan hiçbir seviye (PC-08 B, PC-09 B, PC-11 B, PC-04 A dahil) Product Spec requirement'ı haline gelmez. | Öneri ile kararın karışması, seçim yapılmadan kapsamın sabitlenmesine yol açar. | 2026-08-27 | Kullanıcı |
| **D-022** | FAZ 2B duraklatıldı | İlk dört cluster'ın seviyelendirme yaklaşımı onaylandı. **Kalan 10 cluster'a geçilmiyor.** İlk dört cluster artık araştırmayla değil iş/ürün kararlarıyla ayrıştığı için **FAZ 3 — Interactive Requirement Discovery** başlatıldı. FAZ 2B, FAZ 3 kararlarından sonra devam edecek. | Cevaplanmamış iş kuralları üzerine kapsam seviyesi üretmek, sonradan çöpe gidecek analiz üretir. | 2026-08-27 | Kullanıcı |
| **D-023** | YENİ-01/02/03'ün fazı | Üçü de **FAZ 3 Product/UX kararıdır**, FAZ 4'e bırakılmayacak: **YENİ-01** lokasyon modeli · **YENİ-02** oda oluşturma tek ekran mı sihirbaz mı · **YENİ-03** dolu odalar gizlensin mi devre dışı mı gösterilsin. | Üçü de ürün davranışını değiştiriyor, tasarım detayı değil. | 2026-08-27 | Kullanıcı |
| ~~**D-024**~~ ⛔ | ~~FAZ 3 çalışma biçimi~~ **— D-029 ile geçersiz kılındı** | **Her mesajda yalnızca bir anlamlı ürün kararı sorulur.** Format: Problem → mevcut Narbulut bağlamı → benchmark → seçenekler → Claude önerisi → tek soru. Cevap sonrası: karar DECISIONS.md'ye kaydedilir, ilgili solution option güncellenir, sonraki soru sorulur. **Padding/border-radius/ikon konumu gibi tasarım detayları sorulmaz** — bunlar FAZ 6 Design Brief'e aittir. | Toplu soru listesi, kullanıcıyı bağlamsız karar vermeye zorluyor ve cevapların birbirini etkilemesini engelliyor. | 2026-08-27 | Kullanıcı |
| **D-025** ⚠️ | Organizasyon kapsamı **(PROVISIONAL)** | **Narbulut Takvim'in temel çalışma sınırı bir organizasyondur.** Takvimler, odalar, gruplar ve kullanıcılar aynı organizasyon context'i içinde yaşar. *"Tüm kullanıcılar"* = o organizasyonun kullanıcıları. **Bayi tarafında çapraz-organizasyon görünürlük ve bayi için bağımsız takvim çekirdek kapsama girmiyor.** ⚠️ **Bu kesin business truth değil, provisional product decision'dır.** Yeniden açılma tetikleyicisi: şirketten veya mevcut ürün davranışından **bayi kullanıcılarının kendi bağımsız takvim/oda kullanımına sahip olduğu** doğrulanırsa kapsam yeniden değerlendirilir. | `POST /organization/transactions/calendar` endpoint'i takvimi organization altında isimlendiriyor `[O]`; org değiştirici bayinin *kendi* takvimi olduğunu değil *başka* organizasyona geçtiğini gösteriyor `[I]`. Çapraz-tenant görünürlük PC-11'e tamamen yeni bir eksen ekler ve benchmark'ta normal bir takvim özelliği değil `[I]`. Tek-organizasyon çekirdeği, bayi katmanını sonradan eklemeyi kapatmıyor; tersi doğru değil. | 2026-08-27 | Kullanıcı (A-03) |
| **D-026** | Oda erişim yetkisi tiplenmesi | **Oda erişimi iki ayrı yetkiye ayrılır: "Görebilir" ve "Rezerve edebilir".** Bir kullanıcı/grup bir odayı yalnızca görebilir, ya da görüp rezerve edebilir. **Rezerve edilemeyen oda listeden kaybolmaz:** planlama sırasında oda ve müsaitlik bilgisi görünür kalır, yalnızca seçim/rezerve etme aksiyonu kapalıdır. **"Onaylayabilir" üçüncü yetkisi şimdilik EKLENMİYOR** — PC-10 Reservation Approval FAZ 3'te doğrulanırsa bu modelin üzerine üçüncü yetki olarak eklenebilir. **Migration:** mevcut erişimi olan kullanıcı/gruplar → *Görebilir + Rezerve edebilir* olarak taşınır; mevcut davranış bozulmaz. | `FN-10` (model tipsiz ACL) + `FN-17` (varsayılan herkese açık) birlikte "görünür ama kısıtlı" kavramını imkânsız kılıyordu. Skedda'nın açık ifadesi `[O]`: *"bir mekânı görebilmek onu rezerve edebilme yetkisi anlamına gelmez."* Ayrıca PC-09'un doğru davranması buna bağlı: tek tipli modelde tek seçenek odayı tamamen gizlemekti. B→C geçişi eklemeli, yıkıcı değil. | 2026-08-27 | Kullanıcı (A-01) |
| **D-027** | Takvim görünürlük modeli (free/busy) | **Aynı organizasyondaki kullanıcılar birbirlerinin takviminde yalnızca *müsait / meşgul* bilgisini görür.** Varsayılan olarak **görünmez**: etkinlik başlığı · katılımcılar · oda/lokasyon · notlar ve diğer event detayları. Bu, **PC-07 Scheduling için temel visibility modelidir.** Kullanıcı bazlı kademeli paylaşım (Google/Outlook'un 4–5 kademesi) **şimdilik eklenmiyor**; ileride business requirement çıkarsa bu modelin üzerine genişletilebilir. **Event-level privacy ayrı bir karar olarak açık bırakıldı** (normal event → free/busy'de "meşgul" görünür; özel/gizli event'te detay zaten görünmez — tam davranış ilgili turda konuşulacak). | Benchmark'ın en net bulgusu `[O]`: free/busy karşı tarafın paylaşımına bağlı; paylaşım yoksa özellik sessizce boş çalışıyor — yani PC-07 bu karar olmadan kurulamaz. Free/busy, gizlilik ile koordinasyon gerilimini tek kavramla çözüyor ("ne zaman" evet, "ne" hayır) ve her incelenen üründe en alt kademe olarak duruyor `[O]`. Kademeli paylaşımın maliyeti gerçek: Outlook'un "Custom" durumu granülerliğin okunabilirliği nasıl bozduğunun kanıtı `[O]`. | 2026-08-27 | Kullanıcı (A-02) |
| **D-028** | Oda lokasyon modeli | **Yapılandırılmış Bina + Kat modeli** benimsenir (kat için okunabilir etiket dahil). **Ancak zorunlu değildir:** küçük/tek lokasyonlu organizasyon doğrudan oda oluşturabilir; birden fazla bina/kat kullanan organizasyon Bina → Kat → Oda hiyerarşisini kullanabilir. **Progressive disclosure kuralı:** Bina/Kat verisi tanımlıysa Room Discovery'de filtre olarak kullanılır; tanımlı değilse **ilgili filtreler arayüzde gösterilmez.** Serbest çoklu etiket sistemi (C) **şimdilik eklenmiyor**; gerçek business requirement oluşursa bu modelin üzerine eklenebilir. Event formundaki serbest "Lokasyon" alanının oda seçimiyle ilişkisi **bu karara dahil değildir** — UX-31 ayrı turda konuşulacak. | Serbest metin lokasyon filtrelemeyi fiilen imkânsız kılıyor ("3. Kat" / "3.kat" / "Kat 3" farklı değerler) `[O]`. Microsoft'un dokümanı City/Floor/Capacity doldurulmazsa filtrelerin çalışmadığını yazıyor `[O]`; Room Finder'ın kanıtlanmış akışı bina → kat → filtre `[O]`. Kat, binadan daha sık gerekiyor — tek binalı organizasyonda bile operasyonel olarak anlamlı boyut. Zorunlu tutulmaması, tek yerleşkeli müşteride hiyerarşi yükünü ortadan kaldırıyor. C'nin ikinci gruplama ekseni, mevcut taksonomi karmaşası (UX-18/19/20) çözülmeden risk. | 2026-08-27 | Kullanıcı (YENİ-01) |
| **D-029** | FAZ 3 çalışma biçimi **(D-024'ü geçersiz kılar)** | **Batched Decision Mode.** Sorular tek tek değil, **4–6 soruluk karar paketleri** halinde sorulur; her paket aynı konu/cluster ailesinden gelir. Kısa format: `[konu] / A–B–C / Öneri + 1–2 cümle gerekçe / gerekirse tek satır dependency-risk uyarısı`. Kullanıcı `1A, 2B, 3B…` şeklinde toplu cevaplar. **Yalnızca high-impact kararlar sorulur:** ürün kapsamını değiştiren · business rule belirleyen · permission/data modelini etkileyen · önemli kullanıcı akışını değiştiren · backend/domain ihtiyacını değiştiren · birden fazla gerçekten geçerli yaklaşımı olan. **Sorulmayacaklar:** buton konumu, küçük validation davranışları, empty state metni, ikon tercihi, spacing, hover detayları, basit UI detayları — bunlar audit + benchmark + mevcut kararlar doğrultusunda *recommended davranış* olarak belirlenip **mini-spec review'da topluca** gösterilir. **Decision budget: FAZ 3 toplam ~12–18 karar** (verilmiş kararlar dahil). Yeni soru üretmeden önce *"bu karar gerçekten ürün sahibinin müdahalesini gerektiriyor mu?"* kontrolü yapılır. | FAZ 3'ü gereksiz uzatmamak; ürün sahibinin zamanını yalnızca gerçekten müdahale gerektiren kararlara ayırmak. | 2026-08-27 | Kullanıcı |
| **D-030** | Oda varsayılan erişimi (FN-17) | **Yeni odalar varsayılan olarak herkes tarafından görülebilir ve rezerve edilebilir. Kısıtlama istisnadır.** | Odaların çoğu ortak kaynak; kısıtlamayı varsayılan yapmak her oda oluşturmaya yapılandırma adımı ekler ve yetki verilmesi unutulan oda sessizce kullanılamaz hale gelir. | 2026-08-28 | Kullanıcı (1A) |
| **D-031** | Dolu oda gösterimi (YENİ-03) | **Seçili saatte dolu odalar listeden kaybolmaz; "Dolu" olarak görünür ancak seçilemez.** Sonraki müsait zamanın gösterilmesi **şimdilik eklenmiyor.** | D-026 ile konan "rezerve edilemeyen oda kaybolmaz" ilkesiyle tutarlı. Sonraki-müsait-aralık hesabı ek maliyet; B→C geçişi eklemeli. | 2026-08-28 | Kullanıcı (2B) |
| **D-032** | Oda formu modeli (YENİ-02) | **Oda oluşturma ve düzenleme tek ekranda, bölümlere ayrılmış form olarak yapılır. Sihirbaz kaldırılabilir.** Adım 3'ün gözden geçirme işlevi (UX-51) inline özet/doğrulamaya dönüşür. | Oda düzenleme akışı zaten kurulacak (UX-42) ve mevcut bir odayı sihirbazla düzenlemek tuhaf; tek ekran oluşturma ile düzenlemeyi aynı yüzeyde birleştiriyor. | 2026-08-28 | Kullanıcı (3A) |
| **D-033** | Etkinlikte konum modeli (UX-31) | **Event tarafında tek "Konum" mantığı olur.** Oda seçilmişse konum **odadan otomatik türetilir** ve çelişen serbest metin oluşamaz. Oda seçilmemişse dış/fiziksel lokasyon serbest girilebilir. | D-028 ile oda yapısal Bina+Kat taşıyor; konum türetilebilir. Tek alan, iki kaynağın çelişmesini yapısal olarak imkânsız kılıyor. | 2026-08-28 | Kullanıcı (4A) |
| **D-034** ⚠️ | Rezervasyon onayı — **PC-10 CONDITIONAL gate AÇILDI** | **Onay sistemi üründe var olacak, ancak ilk versiyonda basit ve oda-bazlı.** Bir odada **"Rezervasyon onayı gerekli"** açılıp kapatılabilir; açıkken **onaylayıcı kullanıcı/grup tanımlanabilir**. Akış: **Rezervasyon talebi → Beklemede → Onaylandı / Reddedildi.** **İlk versiyonda EKLENMEYECEK enterprise rule-engine davranışları:** haftanın gününe göre farklı kural · kullanıcı tipine göre farklı policy · maksimum süreye göre approval · karmaşık approval chain · çok aşamalı onay. | Benchmark net `[O]`: onay bir *oda özelliğidir*, global mod değil (Microsoft, Google, Skedda üçünde de). Basit tutmak PC-10'u üründe var edip kapsamı gereksiz büyütmüyor. Skedda'nın granüler kural motoru (gün/tag bazlı) bilinçli olarak dışarıda bırakıldı. | 2026-08-28 | Kullanıcı (5B) |
| **D-035** | D-034'ün diğer kararlarla etkileşimi *(türetilmiş)* | **(a)** Onay açık bir odada, *Rezerve edebilir* yetkisi fiilen **"talep gönderebilir"**e dönüşür (D-026 + D-030 ile tutarlı: herkes talep gönderebilir, onay kapıyı tutar). **(b)** Onaylayıcı, **odanın onay yapılandırmasının bir alanı** olarak tanımlanır; D-026'da ertelenen *"Onaylayabilir"* üçüncü yetki tipi **izin modeline eklenmez** — bu, D-034'ün "basit oda-bazlı" kapsamıyla tutarlı okumadır. **(c)** D-034, `PC-08 C`'nin yalnızca *onay bayrağı + onaylayıcı* kısmını açar; maksimum süre / rezervasyon penceresi / mesai penceresi kural alanları **kapsam dışı kalır**. | Kararlar arası tutarlılığı açık tutmak; kapsamın sessizce genişlemesini önlemek. **(b) Claude yorumudur** — kullanıcı düzeltirse güncellenecek. | 2026-08-28 | Claude, kullanıcı onayına açık |
| **D-036** | Bekleyen rezervasyonun oda müsaitliğine etkisi | **Onay bekleyen rezervasyon ilgili zaman aralığını geçici olarak tutar.** Oda durumu kullanıcıya ayrıştırılmış gösterilir: **Müsait · Onay bekliyor · Rezerve.** Unutulan taleplerin odayı süresiz kilitlememesi için **expiration/auto-cancel davranışı mini-spec'te öneri olarak getirilecek** — ayrı product decision açılmadı. | Microsoft'ta `AutoUpdate` modunda toplantı istekleri delege onaylayana kadar takvimde tentative duruyor `[O]`. Bloke etmemek "onaylandı sandım" karmaşası üretir. | 2026-08-28 | Kullanıcı (1A) |
| **D-037** | Etkinlik tipi taksonomisi (A-05) | **"Toplantı" / "Etkinlik" ayrımı kaldırılır. Core model tek bir "Etkinlik"tir.** Ayrı bir *Toplantı* entity/type yaratılmaz. **Etkinliğin davranışını kullanılan alanlar belirler** (katılımcı, oda, lokasyon vb.). İleride kategori ihtiyacı çıkarsa bu, event type olarak değil **kategori/etiket sistemi** olarak ayrıca değerlendirilir. | Mevcut ayrımın davranışsal karşılığı hiçbir yerde görünmüyor: filtrede iki etiket ayırt edilemiyor (`UX-18`), form başlığı zaten birleşik. Davranış farkı yokken iki kavramı korumak renk/filtre karmaşasını (`UX-19/20`) sürdürüyor. | 2026-08-28 | Kullanıcı (2A) |
| **D-038** | Etkinlik oluşturma katman modeli | **İki katmanlı oluşturma.** Slota tıklanınca **Quick Create**: başlık · tarih/saat · Oluştur. **"Daha fazla seçenek"** ile detaylı forma geçilir. Detay yüzeyinin drawer/modal davranışı **ayrıca netleştirilecek**, görsel tasarım kararıyla karıştırılmayacak. | Bu benchmark setinde güçlü ortak yaklaşım: Google `[O]`, Notion `[O]`. Izgarada inline düzenleme, sürükle-bırak kararı verilmeden erken. | 2026-08-28 | Kullanıcı (3B) |
| **D-039** | Etkinlik düzenleme/silme yetkisi (A-09) | **Organizatör + organizasyon yöneticisi.** Normal durumda sahip organizatördür; organizasyon yöneticisi gerektiğinde müdahale eder. **Katılımcılara düzenleme hakkı verilmez.** **Oda onaylayıcısının event düzenleme hakkı bu karardan türemez** — onaylayıcı yalnızca oda rezervasyon kararını yönetir. | Outlook'ta yalnızca organizatör güncelleme/iptal gönderebiliyor `[O]`; ancak saf "yalnızca organizatör" organizatör ayrıldığında etkinliği yönetilemez bırakır. Katılımcı düzenlemesi sorumluluğu belirsizleştirir ve bildirim gürültüsü üretir. | 2026-08-28 | Kullanıcı (4B) |
| **D-040** | Katılımcı modeli | **Zorunlu / Opsiyonel katılımcı ayrımı eklenir.** İş modelinde **iç kullanıcı / harici (misafir) ayrımı korunur**. Mevcut üç katmanlı tekrar (*Kullanıcılar + Misafir Kullanıcılar + Tüm Katılımcılar*) korunmaz (`UX-32`). ⚠️ **UI kısıtı değil:** iç/harici ayrımının arayüzde iki ayrı dropdown olması **requirement değildir**; tek bir "Katılımcılar" alanı altında iç kullanıcı araması ile harici e-posta eklemeyi birleştirmek solution/spec aşamasında değerlendirilecek. | Zorunlu/opsiyonel ayrımı benchmark'ta öneri motorunu ve free/busy okumasını besliyor `[O]`; ileride Scheduling / Suggested Time davranışında kullanılacak. İç/harici ayrımı `KEEP-06`. | 2026-08-28 | Kullanıcı (5C) |
| **D-041** | Event-level privacy (P-016) | **Ayrı gizlilik seviyesi (Normal / Özel / Gizli) eklenmez.** D-027 gereği diğer kullanıcılar zaten yalnızca free/busy görüyor. ⚠️ **Takvimde tamamen görünmeyen "gizli" event istenmiyor** — free/busy ve conflict detection'ı bozar. | D-027 varsayılan olarak "ne zaman meşgul evet, ne yaptığın hayır" veriyor; ek gizlilik seviyesinin koruma marjı düşük. Görünmez ama var olan etkinlik PC-07'nin temelini oyar. | 2026-08-28 | Kullanıcı (6A) |
| **D-042** ✅ | D-035(b) onaylandı | **"Onaylayabilir" PC-11'e üçüncü genel permission tipi olarak EKLENMEZ.** Approval-enabled odada *Rezervasyon onayı gerekli* ve *Onaylayıcı kullanıcı/grup* **oda seviyesinde** tanımlanır — bir kişi tüm sistem için "approver" olmak yerine **belirli oda/odaların onaylayıcısı** olur. İleride merkezi approval rolleri gerekirse model genişletilebilir. | Claude'un D-035(b) yorumu kullanıcı tarafından açıkça onaylandı. D-034'ün "basit oda-bazlı" kapsamıyla tutarlı; PC-11'i genişletmeden PC-10'u mümkün kılıyor. | 2026-08-28 | Kullanıcı |
| **D-043** | Tekrarlayan etkinlik (FN-04 / C-2) | **Temel recurrence kapsamda.** İlk kapsam: **günlük · haftalık · aylık**; bitiş koşulu: **bitiş tarihi / tekrar sayısı / süresiz**. Seri düzenlemede **yalnızca bu etkinlik · bu ve sonraki etkinlikler · tüm seri** desteklenir. **Kapsam dışı:** "ayın ikinci Salısı" tipi gelişmiş kurallar, karmaşık istisna kuralları. Recurrence'ın oda rezervasyonu/onayıyla etkileşimi **mini-spec içinde çözülecek**, sistem gereksiz enterprise karmaşıklığına çıkarılmayacak. | `FN-04` doğrulanmış boşluk — test notu açıkça *"her etkinliği tek tek oluşturuyorsunuz"* diyor `[O]`. Üçlü kapsam seçimi bu benchmark setinde güçlü ortak yaklaşım (Google `[O]`). Gelişmiş kurallara Narbulut'ta talep kanıtı yok. | 2026-08-28 | Kullanıcı (1B) |
| **D-044** | Bildirim kanalları (A-04) | **E-posta + uygulama içi bildirim, iki kanal da desteklenir.** **Her olayın her iki kanaldan gitmesi requirement değildir** — hangi olayın yalnız e-posta / yalnız uygulama içi / ikisi birden üreteceği **FAZ 8 Calendar Email & Notification Matrix** sırasında belirlenecek. **Misafir kullanıcılar için e-posta temel kanaldır.** | Misafir kullanıcılar uygulamada yok (`KEEP-06`, D-040) — davet onlara ancak e-postayla ulaşır; tek-kanal uygulama içi yapısal olarak yetersiz. D-034 onay akışı durum bildirimini zaten zorunlu kılıyor. | 2026-08-28 | Kullanıcı (2C) |
| **D-045** | Mesai saatleri (PC-06) | **Mesai saatleri organizasyon seviyesinde tanımlanabilir.** Takvim **varsayılan olarak mesai saatlerine scroll eder** ve **mesai dışı saatleri görsel olarak geri plana atar**. ⚠️ **Mesai dışında etkinlik/oda rezervasyonu oluşturmak engellenmez.** **Kapsam dışı:** resmî tatil takvimi (C-6), mesai dışı rezervasyon yasağı (C-7). | `UX-04` (ızgara gece yarısında açılıyor; ilk görülen şey boş gece saatleri) audit'in 3 numaralı bulgusuydu ve bu seviye onu tek başına çözüyor. Tatil/yasak maddeleri AKM notundan geliyor — D-002 gereği otomatik requirement değil; ayrıca D-035(c) oda mesai penceresini zaten kapsam dışı bırakmıştı. | 2026-08-28 | Kullanıcı (3B) |
| **D-046** | Zaman dilimi (A-11) | **İlk kapsamda organizasyonun tek bir timezone'u vardır**; event zamanları bu timezone üzerinden işlenir. Kullanıcı bazlı farklı timezone / çok ülke scheduling **core scope'a girmez**. ⚠️ **Kısıt:** veri/model tasarımında ileride kullanıcı bazlı timezone desteğine geçişi gereksiz yere zorlaştıracak karar alınmayacak. | D-025 ile tek organizasyon sınırı kabul edildi; çok ülkeli kullanım kanıtı yok. Sonradan eklemek mevcut her etkinliğin yorumunu değiştirdiği için geçiş yolu açık bırakılıyor. | 2026-08-28 | Kullanıcı (4A) |
| **D-047** | Mobil / responsive kapsamı (A-12) | **Responsive, desktop-first.** Ana deneyim masaüstü öncelikli; haftalık/günlük grid, oda yönetimi, izinler ve rezervasyon yönetimi masaüstünde en güçlü deneyimi verir. **Tablet ve mobil kapsam dışı değildir.** Mobilde en az şu akışlar kullanılabilir olmalı: etkinlik görüntüleme · günler arası gezinme · etkinlik oluşturma/düzenleme · oda arama · oda rezervasyonu · rezervasyon durumunu görme. ⚠️ **Mobil görünüm masaüstü haftalık grid'i birebir küçültmek zorunda değildir** — agenda/day/list gibi daha uygun responsive pattern kullanılabilir. **Kapsam dışı:** ayrı native mobil uygulama, tamamen mobile-first ürün. | Takvim ızgarası mobilde doğası gereği zor, ancak oda rezervasyonu tipik olarak hareket halinde yapılan bir iş. FAZ 7 prototip kapsamını doğrudan belirliyor. | 2026-08-28 | Kullanıcı (5B) |
| **D-048** | Oda kapasitesi ve özellikleri (PC-08 B doğrulaması) | **Kapasite ve oda özellikleri/donanım alanları kapsamdadır.** PC-09 Room Discovery B'nin anlamlı çalışması için kullanılacak. ⚠️ **İlk kapsamda gereksiz taxonomy veya serbest etiket sistemi kurulmayacak** — yönetilebilir, sınırlı bir özellik seti (ör. projeksiyon/ekran · video konferans · beyaz tahta · erişilebilirlik). Tam liste Product Spec sırasında netleştirilecek. | Claude'un taşıdığı türetilmiş varsayım kullanıcı tarafından doğrulandı. Benchmark'ta kapasite + özellik, oda filtrelemesinin çekirdeği `[O]`; D-028'in yapısal lokasyon kararı bunları zaten ima ediyordu. | 2026-08-28 | Kullanıcı |
| **D-049** ⚠️ | "Özel Takvim" tanımı (A-06 / P-020) — **D-067 ile KISMEN SUPERSEDE EDİLDİ** | ⚠️ *Bu kararın "gelişmiş takvim paylaşımı kapsam dışı" kısmı **D-067 ile değiştirilmiştir**: sınırlı, tek seviyeli, salt okunur takvim paylaşımı artık kapsamdadır. Delegation, çapraz-organizasyon paylaşımı ve karmaşık permission sistemi **kapsam dışı kalmaya devam eder**. Kararın geri kalanı (takvim = isim + renk, PC-03'ün temeli) aynen geçerlidir.* **Takvim = basit etkinlik gruplama / takvim kaynağı.** Takvim **isim + renk** taşır; etkinlik bir takvime atanabilir. **Bu, PC-03 Taksonomi/Renk'in temelidir.** **Kapsam dışı:** gelişmiş takvim paylaşımı · delegation · çapraz-organizasyon paylaşımı · karmaşık permission sistemi. Kişisel/organizasyonel takvim ayrımının **terminolojisi ilgili spec içinde önerilecek**; **yeni subsystem oluşturulmayacak.** | D-037 event type'ı kaldırdığı için renk kaynağı büyük ölçüde takvim oldu; takvimin ne olduğu tanımsızdı ve PC-03'ü bloke ediyordu. Basit gruplama, çoklu takvim modelinin temelini kurarken yeni bir izin yüzeyi açmıyor. | 2026-08-28 | Kullanıcı (P-020) |
| **D-050** | Scheduling üst katmanı (P-021) | **Balanced scheduling kapsamdadır.** İçerir: **event formunda katılımcı müsaitliği** · **zorunlu/opsiyonel ayrımının hesaba katılması** (D-040) · **çakışmaların görünür gösterilmesi** · **Suggested Times / uygun zaman önerileri**. ⚠️ **Tam ölçekli ayrı bir "Scheduling Assistant" ekranı/subsystem'i oluşturmak zorunlu değildir.** Öncelik: **etkinlik oluşturma deneyiminin içinde** doğru zamanı bulmayı kolaylaştırmak. İleride ihtiyaç çıkarsa ayrı Scheduling Assistant yüzeyi eklenebilir. | D-027 (free/busy) ve D-040 (zorunlu/opsiyonel) temeli kurmuştu; üst katman kararsızdı ve Scheduling spec'ini bloke ediyordu. Benchmark'ta ızgara (Outlook) ile öneri listesi (Google) iki ayrı yaklaşım — "form içinde öneri" ikisinin arasında, ayrı ekran maliyeti olmadan değer veren nokta. | 2026-08-28 | Kullanıcı (P-021) |
| **D-051** ✅ | FAZ 3 kapanışı | **FAZ 3 — Interactive Requirement Discovery kapanmıştır.** Toplam 23 ürün/iş kararı (D-025…D-050). Bütçe aşımı kabul edildi; türeyen kararların çoğu PC-10 approval kararının doğal sonucuydu. Kalan iş: FAZ 2B Scope Closure Pass → FAZ 4 Product Spec. | — | 2026-08-28 | Kullanıcı |
| **D-052** | Approval-required oda onaylayıcısız kalamaz | ⚠️ **INVARIANT:** Bir odada `Rezervasyon onayı gerekli = açık` ise **en az bir geçerli onaylayıcı kullanıcı veya grup tanımlı olmak zorundadır.** Onay açık + onaylayıcı boş oda **kaydedilemez**; alan bazlı açık validasyon gösterilir (*"Rezervasyon onayı açık olduğu için en az bir onaylayıcı seçmelisiniz."*). Mevcut odanın **son onaylayıcısı kaldırılamaz** (onay hâlâ açıkken); işlem engellenir. **Onay kapatılırsa onaylayıcı zorunluluğu kalkar.** | Önceki recommendation (SR-APR-03: "talep Pending kalır") **kalıcı broken-state** üretiyordu: onaylayıcısız bir odada talepler süresiz beklerdi. Form seviyesinde engellemek doğru çözüm. | 2026-08-28 | Kullanıcı |
| **D-053** | Self-approval yasağı | **Talebi oluşturan kullanıcı kendi rezervasyon talebini onaylayamaz.** Kullanıcı aynı zamanda o odanın onaylayıcısıysa **kendi talebinde Onayla/Reddet aksiyonları gösterilmez**; kararı başka bir geçerli onaylayıcı vermelidir. Odanın **tek onaylayıcısı talebi oluşturansa**, sistem talep oluşturma aşamasında uyarır: *"Bu rezervasyon için başka bir onaylayıcı bulunmuyor."* (uyarı, engelleyici değil). ⚠️ **Delegation sistemi kurulmaz; self-approval'ın organizasyon politikasıyla açılması kapsam dışıdır.** | Onay bağımsız bir karar mekanizmasıdır; kendi talebini onaylamak onayı anlamsızlaştırır. Önceki recommendation (SR-APR-07) tersine çevrildi. | 2026-08-28 | Kullanıcı |
| **D-054** | Bekleyen talebin yaşam döngüsü | Bekleyen talep dört yoldan terminal duruma geçer: **(1)** talep eden iptal eder · **(2)** onaylayıcı Approved/Rejected yapar · **(3)** bağlı etkinlik iptal edilir → `Cancelled` · **(4)** **etkinliğin başlangıç zamanı geçmiş ve talep hâlâ `Pending` ise** aktif rezervasyon sayılamaz, terminal duruma geçirilir. Terminal duruma geçen talebin **kaydı silinmez** (audit/bildirim korunur) ve **slotu bloke etmez**. ⚠️ **Ayrı expiration sistemi, scheduler veya kural motoru kurulmaz.** | D-036'da işaretlenen "unutulan talepler odayı süresiz kilitlemesin" ihtiyacının karşılığı; yeni bir alt sistem eklemeden çözüyor. | 2026-08-28 | Kullanıcı |
| **D-055** ✅ | FAZ 4 kapanışı | **`10–19` spec seti FAZ 4 FINAL.** 10 spec, 14 cluster kapsandı (PC-15 hariç — Design Brief Input). 63 spec-level recommendation, 158+ acceptance criteria. Kalan iki taşınan belirsizlik açıkça işaretli: `12` §15 takvim sahipliği (integration/domain recommendation) ve `19` kanal matrisi (FAZ 8). | — | 2026-08-28 | Kullanıcı |
| **D-056** | Eligible approver invariant | ⚠️ **Approval-required bir rezervasyon, talebi oluşturan kullanıcı dışında en az bir eligible approver yoksa `Pending` olarak OLUŞTURULMAZ.** Etkinlik formunda **engelleyici (blocking) validasyon**: *"Bu rezervasyonu onaylayabilecek başka bir kullanıcı bulunmuyor."* Kurtarma yolları: başka oda · onay gerektirmeyen oda · odasız devam et · yöneticiden onay yapılandırmasını değiştirmesini isteme. **Eligible approver** = onaylayıcı listesindeki, talebi oluşturandan farklı kullanıcı (veya grup içinde başka bir üye). ⚠️ **Çözümsüz `Pending` rezervasyon üretilmez.** | Önceki davranış ("talep oluşturulur ama karara bağlanamaz") kalıcı bir çıkmaz üretiyordu — D-052'nin çözdüğü broken-state'in başka bir biçimi. Engelleme talep aşamasına alındı. | 2026-08-28 | Kullanıcı |
| **D-057** | Oda yönetimi bildirim üretmez | **Oda adı · kapasite · özellik değişikliği ve izin yönetimi için yeni notification domain event'i EKLENMEZ.** F4 Room Administration akışının bildirim üretmemesi **bilinçli olarak kabul edilmiştir**. Gerçekten son kullanıcıyı etkileyen özel admin değişiklikleri gerekirse **FAZ 8 notification matrix** sırasında yeniden değerlendirilir. **Blocker değildir.** | Yönetim değişikliklerinin çoğu son kullanıcı için görünmez; bildirim üretmek gürültü olur. | 2026-08-28 | Kullanıcı |
| **D-058** ✅ | FAZ 5 kapanışı | **`20-ux-flows.md` FAZ 5 FINAL.** Beş ana flow, cross-flow state map, durum sahipliği tablosu ve 27 Design Handoff Question. | — | 2026-08-28 | Kullanıcı |
| **D-059** ✅ | FAZ 6A kapanışı | **`21-calendar-design-brief.md` onaylandı.** Eksik görsel girdiler (Narbulut marka asset'leri · platform shell referansı · yoğun takvim datası · gerçek oda envanteri · katılımcı yoğunluğu) **blocker değildir**; ilk design exploration bunları beklemez. Geldiğinde final direction iyileştirilebilir. | — | 2026-08-28 | Kullanıcı |
| **D-060** | Direction C marka kısıtı | ⚠️ **Gerçek Narbulut marka asset'i yoksa resmî renk/font/brand system UYDURULMAZ.** Direction C **nötr, uyarlanabilir B2B SaaS design token'ları** kullanır ve bunların **placeholder olduğu açıkça belirtilir.** Amaç marka uygulamak değil, **marka uygulanabilir bir iskelet** kurmaktır. | Uydurulmuş marka rengi/tipografisi, sonradan gerçek marka geldiğinde tüm direction'ı geçersiz kılar ve yanlış bir "onaylanmış marka" izlenimi üretir. | 2026-08-28 | Kullanıcı |
| **D-061** | Design variant kuralı | **Dört direction'ın feature set'i, business rules, kullanıcı rolleri ve ana flow'ları AYNIDIR.** Yalnızca **information hierarchy · density · layout · component treatment · navigation presentation · visual language · progressive disclosure** farklılaşır. ⚠️ **Bir direction'a başka direction'da olmayan feature eklenmez.** Bu bir feature comparison değil, **design direction comparison**'dır. İlk turda 11 surface × 4 direction üretilmez; dört direction da **aynı 6 anchor screen** setini ve **aynı sample data**'yı kullanır. | Farklı içerik veya farklı feature set, karşılaştırmayı imkânsız kılar; kullanıcı hibrit seçim (ör. "shell A, form B, room picker D") yapabilmelidir. | 2026-08-28 | Kullanıcı |
| **D-062** | Tablet breakpoint | **İlk design exploration'da tablet breakpoint EKLENMEZ.** Desktop ana yüzey + ~390px mobil yeterlidir. Tablet adaptasyonu **direction seçildikten sonra** final responsive design aşamasında ele alınır. | Dört direction × altı ekran zaten geniş bir keşif; üçüncü breakpoint karşılaştırmayı zorlaştırır ve henüz seçilmemiş bir yön için maliyet üretir. | 2026-08-28 | Kullanıcı |
| **D-063** | Design exploration çalışma kuralları | Prompt'a üç bağlayıcı kural eklendi: **(1) Gerçek ürün durumu keşfi** — Pending/Reserved/occupied/permission-denied/conflict/capacity/recurring/Unknown durumları dekoratif rozet gibi sonradan eklenmiş görünemez; layout ve component hierarchy bunları doğal taşımalı. **(2) Direction içi design-system tutarlılığı** — bir yönün altı ekranı aynı ürün ailesinden görünmeli, bağımsız konsept çalışmaları gibi değil. **(3) Clarification yasağı** — Claude Design soru sormadan best-effort karar alır; eksik marka asset'i, breakpoint veya görsel tercih clarification sebebi değildir, yalnızca doğrudan ürün gereksinimi çelişkisi bildirilebilir. | (1) Durumlar sonradan eklenirse tasarım gerçek kullanımda bozulur. (2) Ekranlar tutarsızsa hibrit seçim (ör. "shell A + form B") anlamsızlaşır. (3) Design question'ların amacı variant üretmek, kullanıcıya soru döndürmek değil. | 2026-08-28 | Kullanıcı |
| **D-064** | Takvim sahipliği (§15-R1 KAPANDI) | **Takvim bir kullanıcıya aittir; sahibi takvimi oluşturan kişidir.** Bu, `12-calendars-spec.md` §15-R1'de FAZ 3'te açık bırakılan sorunun **canonical cevabıdır**. ⚠️ Bu karar tek başına takvim paylaşımını **etkinleştirmez** — sharing ayrı bir karar paketiyle değerlendiriliyor. **Bağımlı açık konular:** §15-R2 (varsayılan takvim), §15-R3 (atama zorunluluğu), §15-R4 (kullanıcı organizasyondan ayrılırsa). | Calendar Sharing capability talebi ownership'i zorunlu kıldı: paylaşım bir sahip olmadan tanımlanamaz (kim paylaşır, kim kaldırır). §15-R1'in önerdiği yön; D-025'in organizasyon sınırıyla uyumlu. | 2026-08-31 | Kullanıcı (Q1-A) |
| **D-065** | Varsayılan takvim (§15-R2 **ve** §15-R3 KAPANDI) | **Her kullanıcı için otomatik bir varsayılan takvim oluşur.** **Silinemez**; adı ve rengi değiştirilebilir. **Her etkinliğin bir takvimi olmak zorundadır** — takvim seçilmeden oluşturulan etkinlik varsayılan takvime düşer (Quick Create dahil). Bu, §15-R3'ü (*atama zorunlu mu*) **zorunlu atama** olarak kapatır. | Sharing takvim başına yapılıyor; takvimsiz bir etkinlik hiçbir paylaşıma dahil olamazdı → *"takvimimi paylaştım ama Ali etkinliklerimin yarısını görmüyor"* sessiz boşluğu. Audit'in 1 numaralı bulgusunun (durum iletişimi güvenilmez) yeni bir örneği olurdu. | 2026-08-31 | Kullanıcı (R2-A) |
| **D-066** | Kullanıcı organizasyondan ayrılırsa (§15-R4 KAPANDI) | **Kullanıcının sahip olduğu takvimler arşivlenir.** Etkinlikler **korunur**. **Tüm aktif calendar sharing ilişkileri kaldırılır.** Arşivlenen takvim hiçbir kullanıcının sidebar'ında görünmez. ⚠️ **Yeni ayrı admin calendar-management yüzeyi AÇILMAZ**; etkinliklere müdahale mevcut **event-level** admin modeliyle (D-039) devam eder. | `15` EC-EVT-02 emsali: organizatör ayrılırsa etkinlik silinmez, yönetici müdahale eder. Tek gerçek katkı paylaşımların kesilmesi — ayrılan çalışanın takvimi başkasının sidebar'ında asılı kalmamalı. Yeni admin yüzeyi `12` §3'ün "yöneticinin bu modülde ayrıcalıklı rolü yoktur" ilkesini bozardı. | 2026-08-31 | Kullanıcı (R4-B) |
| **D-067** | **Calendar Sharing V1** *(D-049'u kısmen supersede eder)* | **Takvim sahibi, takvimini aynı organizasyondaki tekil bir kullanıcıyla paylaşabilir.** **Tek paylaşım seviyesi: "Etkinlik detaylarını görebilir".** Paylaşım **salt okunurdur** — düzenleme/yönetme yetkisi yoktur. Alıcıda **"Benimle paylaşılanlar"** bölümünde görünür, **kendi takvim rengini** kullanır, alıcı görünürlüğünü kendi sidebar'ında **açıp kapatabilir**. **Sahip paylaşımı istediği zaman kaldırabilir; kaldırma anında etkilidir.** ⚠️ **Kapsam dışı:** free/busy için ayrı sharing tier / subscription / calendar overlay permission · **grup paylaşımı** · delegation · harici kullanıcı · çapraz-organizasyon · public link · edit/manage yetkisi. **Organizasyon içi mevcut free/busy modeli D-027 üzerinden aynen devam eder.** | Free/busy zaten D-027 ile organizasyon genelinde okunabilir ve kapatılamaz; ikinci bir "sadece müsaitlik" tier'ı **zaten verilmiş bir izni tekrar izne bağlardı** ve yanlış zihinsel model kurardı. Tek tier + salt okunur, **yeni permission subsystem'i açmadan** capability'yi mümkün kılıyor. | 2026-08-31 | Kullanıcı (Q2-C, Q5-A) |
| **D-068** | Sharing ve gizlilik — D-041 korunur | **Event-level privacy EKLENMEZ** (D-041 aynen geçerli). Hassas bir etkinlik görünmemeliyse kullanıcı onu **paylaşılmayan başka bir takvimde** tutar. ⚠️ **Zorunlu UX açıklaması:** paylaşım yüzeyi *"Bu takvimdeki mevcut ve gelecekteki etkinlik detayları paylaştığınız kişi tarafından görülebilir"* bilgisini açıkça vermelidir. **Özellikle varsayılan takvim paylaşılırken** bu davranış anlaşılır olmalıdır — çünkü D-065 gereği Quick Create etkinlikleri oraya düşer. | D-041'in gerekçesi (*görünmez ama var olan etkinlik free/busy ve çakışma kontrolünü bozar*) sharing ile geçersizleşmiyor. Çoklu takvim modeli gizlilik mekanizmasını zaten sağlıyor — ikinci bir kavram gereksiz. Artık risk (varsayılan takvime düşen hassas etkinlik) UX açıklamasıyla yönetilir. | 2026-08-31 | Kullanıcı (Q3-A) |
| **D-070** ⚠️ | Rakip rezervasyon talepleri (**D-036'nın "bekleyen talep bloke eder" maddesini geçersiz kılar**) | **Aynı slota birden fazla talep gelebilir.** Kullanıcı talep göndermekten **engellenmez**; bekleyen talep slotu **bloke etmez**, yalnız bilgi olarak gösterilir. **Bir talebin onaylanması diğerlerini otomatik reddetmez** — her biri açık kararla sonuçlanır. ⚠️ Fiziksel çakışmayı önleyen tek kural: **slot kesinleşmişse ikinci onay verilemez**; onaylayıcı sebebi görür ve isterse önce mevcut rezervasyonu kaldırır. *(D-036'nın "unutulan talep odayı süresiz kilitlemesin" gerekçesi de kendiliğinden karşılanır: bekleyen talep zaten kilitlemiyor.)* | D-036 ilk gelenin slotu kapatmasını seçmişti; pratikte iki kullanıcı aynı odayı istediğinde yönetici seçim yapamıyor, ikinci kullanıcı talep bile gönderemiyordu. Ürün sahibi: *"kullanıcı yine yollayabilsin, oda yöneticisinin kararına kalsın; otomatik red olmasın."* | 2026-09-02 | Kullanıcı |
| **D-071** ⚠️ | Oda sorumlusu kesinleşmiş rezervasyonu kaldırabilir (**BR-APR-04'ü daraltılmış biçimde genişletir**) | Oda sorumlusu, **kendi odasındaki** `Reserved` bir rezervasyonu **zorunlu gerekçeyle** kaldırabilir. Etkinlik **silinmez**, odasız kalır; sahibine gerekçeyle bildirim gider (N-RES-06). ⚠️ Bu **kararı geri almak değildir** — talep kaydı geçmişte `approved` kalır (BR-APR-22 yerinde durur); düşen şey rezervasyondur. Yetki **yalnız sorumlu olunan odayla** sınırlıdır; etkinliğin kendisine dokunulamaz. | D-070 otomatik reddi kaldırınca, ikinci onayı engelleyen mesaj *"önce o rezervasyonu kaldırın"* diyordu — ama yöneticinin bunu yapabileceği hiçbir yol yoktu; kullanıcıya **yapamayacağı bir eylem** öneriliyordu. Ayrıca gerçek tesis yönetiminde bakım/acil durum için oda serbest bırakılabilmelidir; aksi hâlde sahibi ulaşılamaz olduğunda oda süresiz kilitlenir. Ürün sahibi: *"gerekçeyle kaldırabilsin."* | 2026-09-02 | Kullanıcı |
| **D-069** | Sharing ve arama | **Detay erişimi verilen paylaşılan takvimlerdeki etkinlikler aramaya dahil edilir.** Arama sonucu gerektiğinde **kaynak takvim / sahip** bilgisini göstermelidir. | `14` BR-SHELL-38'in ilkesi zaten *"erişebildiği etkinlikler"*; detay izni erişim demektir. *"Görebiliyorum ama arayamıyorum"* tutarsız olurdu. ⚠️ Bilinçli bir gizlilik yüzeyi: sahip, etkinlik başlıklarının alıcının arama sonuçlarında çıkacağını bilmelidir (D-068 açıklaması bunu kapsar). | 2026-08-31 | Kullanıcı (Q4-A) |

---

## Beklemedeki kararlar

Henüz verilmemiş, ilgili fazda karara bağlanacak konular.

| ID | Konu | Karar verilecek faz |
|---|---|---|
| P-001 | Hangi improvement candidate'lar Product Spec'e girecek | FAZ 2B kapısı |
| P-011 | PC-08/09/11/04 için seçilecek kapsam seviyeleri (A/B/C) | FAZ 2B adım 2 sonrası |
| P-012 | YENİ-01 lokasyon modeli · YENİ-02 oda formu tek ekran/sihirbaz · YENİ-03 dolu oda gösterimi | **FAZ 3** |
| ~~P-013~~ | ~~A-03 organizasyon kapsamı~~ → **D-025 ile karara bağlandı (provisional)** | ✅ |
| ~~P-014a~~ | ~~A-01 oda erişimi~~ → **D-026 ile karara bağlandı** | ✅ |
| ~~P-014b~~ | ~~A-02 takvim paylaşımı~~ → **D-027 ile karara bağlandı** | ✅ |
| ~~P-014c~~ | ~~YENİ-01 lokasyon modeli~~ → **D-028 ile karara bağlandı** | ✅ |
| ~~P-017~~ | ~~Paket 1 — Rooms & Permissions~~ → **D-030…D-035 ile karara bağlandı** | ✅ |
| ~~P-018~~ | ~~Paket 2 — Event & Scheduling~~ → **D-036…D-042 ile karara bağlandı** | ✅ |
| ~~P-019~~ | ~~Paket 3 — Scope & Cross-cutting~~ → **D-043…D-047 ile karara bağlandı** | ✅ |
| ~~P-020~~ | ~~A-06 Özel Takvim~~ → **D-049 ile karara bağlandı** | ✅ |
| ~~P-021~~ | ~~PC-07 üst katmanı~~ → **D-050 ile karara bağlandı (Balanced)** | ✅ |
| P-022 | A-08 ("Odalara Göre" bugün kullanılıyor mu) · A-10 (HTTP 500 kapsamda mı) — düşük etkili doğrulamalar | Açık, blocker değil |
| P-016 | **Event-level privacy** (normal / özel / gizli event davranışı) — D-027 ile açık bırakıldı | FAZ 3 — ayrı tur |
| P-015 | D-025'in doğrulanması: bayi kullanıcılarının bağımsız takvim/oda kullanımı var mı? | FAZ 3 boyunca açık |
| P-002 | Panel ve rol kapsamı (A-03) | FAZ 3 |
| P-003 | "Toplantı" / "Etkinlik" ayrımının korunup korunmayacağı (A-05) | FAZ 3 |
| P-004 | Özel takvim kavramının tanımı (A-06) | FAZ 3 |
| P-005 | Özel takvim HTTP 500 hatasının bu çalışmanın kapsamında olup olmadığı (A-10) | FAZ 3 |
| P-009 | Oda erişim yetkisinin anlamı ve yetki matrisine geçilip geçilmeyeceği (A-01) | FAZ 3 |
| P-006 | Modül/spec kırılımı (`01-…`, `02-…` dosya yapısı) | FAZ 4 başında |
| P-007 | Claude Design'a gönderilecek tasarım yönleri | FAZ 6 |
| P-008 | Hangi olayların e-posta üreteceği (Calendar Email Matrix) | FAZ 8 |
